{
  "name": "seo-godlike",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.18.0 <27"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/*.test.js && node --test scripts/context-packet.test.mjs",
    "test:focused": "npm run build && node --test dist/pipeline.test.js",
    "start": "node dist/cli.js"
  },
  "devDependencies": {
    "@types/node": "26.1.1",
    "typescript": "7.0.2"
  },
  "dependencies": {
    "@openai/codex-sdk": "^0.146.0"
  }
}
